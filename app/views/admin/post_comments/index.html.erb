<table class="table table-hover mb-0 text-center" style="background-color:#fcfced">
  <thead class="table-active">
    <tr>
              <th><%= "氏名" %></th>
              <th><%= "コメント" %></th>
    </tr>
  </thead>
  <tbody>
  <% @postcomment.each do |comment| %>
<tr>
                <td><%= comment.user.name%></td>
                <td><%= comment.comment %>
                <%= link_to  'Destroy' , admin_post_comment_path(comment.id),method: :delete, class: 'btn btn-danger'  %> </td>
      </tr>
  <% end %>
  </tbody>
  </table>