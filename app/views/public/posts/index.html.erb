<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-md-3 ">
        <%= render 'public/users/info', user: @user %>
        <p class="text-center"><%=link_to"お気に入り記事",favorites_user_path(@user) %></p>
        <%= render 'public/posts/form',post: @post,tag_list: @tag_list %><br><br>
        <%= render 'public/posts/number',posts: @posts %>
      <div class="mt-5">
        <%= render 'public/posts/favorite', post_like_ranks: @post_like_ranks %>
        <div class="mt-5">
        <%= render 'public/posts/tag', tag_like_ranks: @tag_like_ranks %>
        </div>
      </div>
    </div>
    <div class="col-md-8 offset-md-1">
      <h2 class="text-center">投稿記事</h2>
      <% @posts.each do |post| %>
      <div class="card  border-primary mb-3 ">
        <div class="row g-0">
           <div class="col-md-4">
              <%= link_to post_path(post.id) do %>
              <%= image_tag post.get_image ,size:"200x200"%>
              <% end %>
           </div>
           <div class="col-md-8">
            <div class="card-body">
              <div class="col-8">
                <%= image_tag post.user.get_profile_image, size:"30x30", class:"mt-3 rounded-circle"; %>
              </div>
              <div class="col-10 md-2">
                <p class="card-title">
                  <%= link_to(post_path(post.id), class:"text-dark") do %>
                    <strong><i class="far fa-flag"></i><span> <%= post.title %></span></strong>
                  <% end %>
                  </p>
                  <p class="card-text"><%= post.body %></p>
                  <div id="favorite_buttons_<%= post.id %>">
                     <%= render "public/favorites/favorite",post:post %>
                  </div>
              </div>
            </div>
           </div>
        </div>
      </div>
      <% end %>
      <%= paginate @posts %>
    </div>
  </div>
</div>

